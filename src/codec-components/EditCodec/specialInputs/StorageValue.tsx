import { LookupEntry } from "@polkadot-api/metadata-builders"
import { EditBytesInterface } from "@polkadot-api/react-builder"
import { FC } from "react"

export const StorageValue: FC<
  EditBytesInterface & {
    storageEntry: LookupEntry
  }
> = () => {
  return null as any
}
